<h1> Links </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title:  <%= link.title %>
      URL:    <%= link.url %>
      tags:   <%= link.tags.map(&:name).join(' ') %>
    </li>
  <% end %>
</ul>
